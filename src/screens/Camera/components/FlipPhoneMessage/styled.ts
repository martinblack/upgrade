import { View } from 'react-native'
import styled from 'styled-components'

export const Container = styled(View)`
  display: flex;
  padding: 10px;
  flex-direction: row;
  align-items: center;
  align-self: center;
`
