export const CHANNEL_ID_PUSH = 'push-notification'
export const CHANNEL_NAME_PUSH = 'Push Notification Channel'

export const MAX_FREQUENCY_IN_HOURS = 8
export const MIN_DELAY_IN_MINUTES = 15

export enum ActionIds {
  CANCEL = 'cancel',
}
